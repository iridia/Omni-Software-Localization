{
    "views" : {
        "all" : {
           "map": "function(doc) { emit(doc._id, doc); }"
        },
        "openID": {
           "map": "function(doc) { emit(doc.OLUserOpenIDKey, doc); }"
        },
        "username": {
            "map": "function(doc) { emit(doc.OLUserNameKey, doc); }"
        }
    }
}