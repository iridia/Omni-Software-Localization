{
    "views" : {
       "find": {
           "map": "function(doc) {\n  emit(doc._id, {'id':doc._id);\n}"
       },
       "find_by_email": {
           "map": "function(doc) {\n  emit(doc.OLUserEmailKey, {'id':doc._id});\n}"
       }
    }
}