{
    "views" : {
       "find": {
           "map": "function(doc) {\n  emit(doc._id, {'id':doc._id});\n}"
       },
	   "find_by_touserid": {
           "map": "function(doc) {\n  emit(doc.OLMessageToUserIDKey, {'id':doc._id});\n}"
       }
    }
}