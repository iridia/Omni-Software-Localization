{
    "views" : {
       "find": {
           "map": "function(doc) {\n  emit(doc._id, {'id':doc._id});\n}"
       },
       "find_by_name": {
           "map": "function(doc) {\n  emit(doc.OLProjectNameKey, {'id':doc._id});\n}"
       }
    }
}