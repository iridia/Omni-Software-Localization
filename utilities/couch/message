{
    "views" : {
        "all" : {
           "map": "function(doc) { emit(doc._id, doc); }"
        },
        "toUserID": {
           "map": "function(doc) { emit(doc.OLMessageToUserIDKey, doc); }"
        }
    }
}